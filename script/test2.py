import operations
from viewer import Viewer


if __name__ == '__main__':
    envents ={
        'event1':operations.Element(),
        'event2':operations.Element(),
    }

    event1 = operations.Element(),
    event2 = operations.Element(),

    event_queue = EventQueue(__name__)
    event_queue.push(event1, event2, [events, ])

